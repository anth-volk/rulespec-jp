# Independent Japan RuleSpec instructions

- Work only in `anth-volk` repositories. Do not create upstream Axiom issues,
  branches, pull requests, releases, or registry entries.
- The inclusive execution boundary is `2017-04-01`.
- Preserve original Japanese era dates and normalize them to Gregorian ISO
  dates for execution. Never rewrite proof excerpts.
- Canonical policy content belongs under `jp/{statutes,regulations,policies}`.
- Every RuleSpec module requires a same-stem companion test.
- Every monetary parameter requires an exact primary-source proof atom.
- Do not describe the model as complete while the coverage ledger contains an
  unencoded Wave 1 item.
- Do not emit combined disposable income until inhabitant tax and non-uniform
  health and long-term-care premiums are modeled explicitly.
